version: '3'
services:
  license-server:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - ADMIN_KEY=claw-roox-admin-2026
    volumes:
      - ./serviceAccountKey.json:/app/serviceAccountKey.json:ro
    restart: always
